# 010 — Write the v1 specification

Type: `wayfinder:task` (HITL)
Status: CLOSED 2026-08-22, out of scope
Blocked by: 004, 009

## Question

Gather every resolved decision into one specification Jeff can build from, plus
the prioritized roadmap. This is the destination.

The specification must be enough to start typing against: the crate layout, the
core interface, the data table format, the CLI contract, and the invariants.
It must not contain the implementation.

## Answer

Ruled out of scope, not resolved. On 2026-08-22 Jeff withdrew the plan to build
v1 himself, so a specification written for him to build from has no reader. v1
is built. The decisions the specification would have carried are recorded on the
tickets that asked them, each pointing at the code that answers it.

What a reader still needs is a README, which is a different document with a
different audience.
