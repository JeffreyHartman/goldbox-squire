# 012 — The README

Type: `wayfinder:task` (AFK)
Status: open
Blocked by: none

## Question

v1 is built and there is no README. Anyone who finds this repository, Jeff
included, needs one page that says:

- What Goldbox Squire is, and why it exists where Gold Box Companion already
  does. The short answer is that GBC is Windows-only and assumes DOSBox 0.74's
  memory layout, and that Squire assumes nothing about the layout.
- How to build it and how to run it.
- Why `gbs` starts DOSBox rather than attaching to one, in two sentences, so
  the design does not look strange.
- What v1 reads, and what it does not do.
- Where the offsets live, and how to add another game.

This replaces [010](010-write-v1-spec.md), which was a specification for
building v1 and no longer has a reader.

## Answer

<!-- filled on resolution -->
