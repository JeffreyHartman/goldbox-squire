# Toolchain

How the repo is built and checked, as opposed to what it does. Formatting,
linting, pinned versions, and whatever runs them.

No map: these are standalone tickets rather than an effort with a destination.
Numbers continue the repo-wide run, because a commit message citing `044` must
name exactly one ticket across the whole repo.
