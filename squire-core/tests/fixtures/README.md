# Test fixtures

Six character records from a real Pool of Radiance save, written by the game in
2015. They are the party the offset table was verified against.

They live here so that the test suite runs anywhere. Tests that read them must
fail when they are missing, never skip. A test that skips silently reports
success while checking nothing.
